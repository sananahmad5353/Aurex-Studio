import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Aurex Studio | Digital Marketing Agency",
  description: "Full-service digital marketing agency helping businesses grow with data-driven strategies, performance marketing, and brand growth solutions.",
  keywords: ["digital marketing", "performance marketing", "brand growth", "lead generation", "Aurex Studio"],
  authors: [{ name: "Aurex Studio" }],
  openGraph: {
    title: "Aurex Studio | Digital Marketing Agency",
    description: "Full-service digital marketing agency helping businesses grow with data-driven strategies.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}