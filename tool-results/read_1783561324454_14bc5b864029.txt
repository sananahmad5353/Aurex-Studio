     1→     1→'use client';
     2→     2→
     3→     3→import { useState } from 'react';
     4→     4→import {
     5→     5→  X, Save, Plus, Trash2, Edit3, LogOut, Settings, ImageIcon, Layers, KeyRound, Loader2,
     6→     6→  Target, BarChart3, TrendingUp, Rocket, Users, Filter, Handshake, Lightbulb,
     7→     7→  ArrowRight, Zap, Globe, Search, Mail, Megaphone, PieChart, Database,
     8→     8→  Code, Cpu, Smartphone, Palette, PenTool, Monitor, Shield, Star, Heart,
     9→     9→  Award, Bookmark, Briefcase, Calendar, Camera, Cloud, MessageSquare, Eye, EyeOff, Film, Play, Lock, Unlock, QrCode, SmartphoneNfc,
    10→    10→} from 'lucide-react';
    11→    11→import type { LucideIcon } from 'lucide-react';
    12→    12→import { Button } from '@/components/ui/button';
    13→    13→import { Input } from '@/components/ui/input';
    14→    14→import { Textarea } from '@/components/ui/textarea';
    15→    15→import { Label } from '@/components/ui/label';
    16→    16→import { Switch } from '@/components/ui/switch';
    17→    17→import { useToast } from '@/hooks/use-toast';
    18→    18→
    19→    19→const iconMap: Record<string, LucideIcon> = {
    20→    20→  Target, BarChart3, TrendingUp, Rocket, Users, Filter, Handshake, Lightbulb,
    21→    21→  ArrowRight, Zap, Globe, Search, Mail, Megaphone, PieChart, Database,
    22→    22→  Code, Cpu, Smartphone, Palette, PenTool, Monitor, Settings, Shield,
    23→    23→  Star, Heart, Award, Bookmark, Briefcase, Calendar, Camera, Cloud,
    24→    24→};
    25→    25→
    26→    26→interface Slide { id: string; title: string; subtitle: string; imageUrl: string; ctaText: string; ctaLink: string; order: number; active: boolean; }
    27→    27→interface ServiceItem { id: string; title: string; description: string; icon: string; order: number; active: boolean; }
    28→    28→interface TestimonialItem { id: string; name: string; role: string; company: string; content: string; rating: number; imageUrl: string; order: number; active: boolean; }
    29→    29→interface PartnerItem { id: string; name: string; imageUrl: string; website: string; order: number; active: boolean; }
    30→    30→interface ContactMsg { id: string; name: string; email: string; phone: string; subject: string; message: string; read: boolean; createdAt: string; }
    31→    31→interface ReelItem { id: string; platform: string; reelUrl: string; order: number; active: boolean; }
    32→    32→
    33→    33→interface AdminPanelProps { open: boolean; onClose: () => void; }
    34→    34→
    35→    35→type Tab = 'settings' | 'slides' | 'services' | 'reviews' | 'partners' | 'reels' | 'messages' | 'password' | 'security';
    36→    36→
    37→    37→export default function AdminPanel({ open, onClose }: AdminPanelProps) {
    38→    38→  const [token, setToken] = useState<string | null>(null);
    39→    39→  const [email, setEmail] = useState('');
    40→    40→  const [password, setPassword] = useState('');
    41→    41→  const [loginLoading, setLoginLoading] = useState(false);
    42→    42→  const [loginError, setLoginError] = useState('');
    43→    43→  const [activeTab, setActiveTab] = useState<Tab>('settings');
    44→    44→  const [loading, setLoading] = useState(false);
    45→    45→  const { toast } = useToast();
    46→    46→
    47→    47→  const [settings, setSettings] = useState<Record<string, string>>({});
    48→    48→  const [aboutStats, setAboutStats] = useState<{ label: string; value: string }[]>([]);
    49→    49→  const [slides, setSlides] = useState<Slide[]>([]);
    50→    50→  const [editingSlide, setEditingSlide] = useState<Partial<Slide> | null>(null);
    51→    51→  const [slideFormOpen, setSlideFormOpen] = useState(false);
    52→    52→  const [services, setServices] = useState<ServiceItem[]>([]);
    53→    53→  const [editingService, setEditingService] = useState<Partial<ServiceItem> | null>(null);
    54→    54→  const [serviceFormOpen, setServiceFormOpen] = useState(false);
    55→    55→  const [testimonials, setTestimonials] = useState<TestimonialItem[]>([]);
    56→    56→  const [editingTestimonial, setEditingTestimonial] = useState<Partial<TestimonialItem> | null>(null);
    57→    57→  const [testimonialFormOpen, setTestimonialFormOpen] = useState(false);
    58→    58→  const [partners, setPartners] = useState<PartnerItem[]>([]);
    59→    59→  const [editingPartner, setEditingPartner] = useState<Partial<PartnerItem> | null>(null);
    60→    60→  const [partnerFormOpen, setPartnerFormOpen] = useState(false);
    61→    61→  const [messages, setMessages] = useState<ContactMsg[]>([]);
    62→    62→  const [reels, setReels] = useState<ReelItem[]>([]);
    63→    63→  const [editingReel, setEditingReel] = useState<Partial<ReelItem> | null>(null);
    64→    64→  const [reelFormOpen, setReelFormOpen] = useState(false);
    65→    65→  const [aboutImages, setAboutImages] = useState<string[]>([]);
    66→    66→  const [currentPwd, setCurrentPwd] = useState('');
    67→    67→  const [newPwd, setNewPwd] = useState('');
    68→    68→  const [confirmPwd, setConfirmPwd] = useState('');
    69→    69→  // 2FA states
    70→    70→  const [twoFactorRequired, setTwoFactorRequired] = useState(false);
    71→    71→  const [pendingToken, setPendingToken] = useState<string | null>(null);
    72→    72→  const [twoFactorCode, setTwoFactorCode] = useState('');
    73→    73→  const [twoFactorEnabled, setTwoFactorEnabled] = useState(false);
    74→    74→  const [twoFactorSecret, setTwoFactorSecret] = useState('');
    75→    75→  const [twoFactorUri, setTwoFactorUri] = useState('');
    76→    76→  const [twoFactorSetupCode, setTwoFactorSetupCode] = useState('');
    77→    77→  const [twoFactorVerifyCode, setTwoFactorVerifyCode] = useState('');
    78→    78→  const [twoFactorDisableCode, setTwoFactorDisableCode] = useState('');
    79→    79→  const [showTwoFactorSetup, setShowTwoFactorSetup] = useState(false);
    80→    80→
    81→    81→  const h = () => ({ 'Content-Type': 'application/json' as const, Authorization: `Bearer ${token}` as const });
    82→    82→
    83→    83→  const fetchData = async () => {
    84→    84→    const hdrs = h();
    85→    85→    try { const r = await fetch('/api/settings', { headers: hdrs }); const d = await r.json(); setSettings(d); try { setAboutStats(JSON.parse(d.aboutStats || '[]')); } catch { setAboutStats([]); } } catch { /* */ }
    86→    86→    try { const r = await fetch('/api/hero-slides', { headers: hdrs }); if (r.ok) setSlides(await r.json()); } catch { /* */ }
    87→    87→    try { const r = await fetch('/api/services', { headers: hdrs }); if (r.ok) setServices(await r.json()); } catch { /* */ }
    88→    88→    try { const r = await fetch('/api/testimonials', { headers: hdrs }); if (r.ok) setTestimonials(await r.json()); } catch { /* */ }
    89→    89→    try { const r = await fetch('/api/partners', { headers: hdrs }); if (r.ok) setPartners(await r.json()); } catch { /* */ }
    90→    90→    try { const r = await fetch('/api/contact', { headers: hdrs }); if (r.ok) setMessages(await r.json()); } catch { /* */ }
    91→    91→    try { const r = await fetch('/api/reels', { headers: hdrs }); if (r.ok) setReels(await r.json()); } catch { /* */ }
    92→    92→    try { setAboutImages(JSON.parse(settings.aboutImages || '[]')); } catch { setAboutImages([]); }
    93→    93→  };
    94→    94→
    95→    95→  const handleLogin = async () => {
    96→    96→    setLoginLoading(true); setLoginError('');
    97→    97→    try {
    98→    98→      const res = await fetch('/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password, twoFactorCode: twoFactorRequired ? twoFactorCode : undefined, pendingToken: twoFactorRequired ? pendingToken : undefined }) });
    99→    99→      const data = await res.json();
   100→   100→      if (res.ok) {
   101→   101→        if (data.twoFactorRequired) {
   102→   102→          setTwoFactorRequired(true);
   103→   103→          setPendingToken(data.pendingToken);
   104→   104→          setLoginError('');
   105→   105→        } else {
   106→   106→          setToken(data.token);
   107→   107→          setTwoFactorRequired(false);
   108→   108→          setPendingToken(null);
   109→   109→          setTwoFactorCode('');
   110→   110→          toast({ title: 'Logged in successfully' });
   111→   111→          void fetchTwoFactorStatus();
   112→   112→          void fetchData();
   113→   113→        }
   114→   114→      }
   115→   115→      else { setLoginError(data.error || 'Login failed'); }
   116→   116→    } catch { setLoginError('Network error'); }
   117→   117→    setLoginLoading(false);
   118→   118→  };
   119→   119→
   120→   120→  const fetchTwoFactorStatus = async () => {
   121→   121→    if (!token) return;
   122→   122→    try {
   123→   123→      const r = await fetch('/api/auth/2fa', { headers: h() });
   124→   124→      if (r.ok) { const d = await r.json(); setTwoFactorEnabled(d.enabled); }
   125→   125→    } catch { /* */ }
   126→   126→  };
   127→   127→
   128→   128→  const handleSetup2FA = async () => {
   129→   129→    setLoading(true);
   130→   130→    try {
   131→   131→      const r = await fetch('/api/auth/2fa', { method: 'POST', headers: h() });
   132→   132→      const d = await r.json();
   133→   133→      if (r.ok) {
   134→   134→        setTwoFactorSecret(d.secret);
   135→   135→        setTwoFactorUri(d.uri);
   136→   136→        setTwoFactorSetupCode(d.testCode);
   137→   137→        setShowTwoFactorSetup(true);
   138→   138→      } else { toast({ title: 'Error', description: d.error, variant: 'destructive' }); }
   139→   139→    } catch { toast({ title: 'Error', variant: 'destructive' }); }
   140→   140→    setLoading(false);
   141→   141→  };
   142→   142→
   143→   143→  const handleVerify2FA = async (action: 'enable' | 'disable') => {
   144→   144→    const code = action === 'enable' ? twoFactorVerifyCode : twoFactorDisableCode;
   145→   145→    if (!code) { toast({ title: 'Enter the 6-digit code', variant: 'destructive' }); return; }
   146→   146→    setLoading(true);
   147→   147→    try {
   148→   148→      const r = await fetch('/api/auth/2fa', { method: 'PUT', headers: h(), body: JSON.stringify({ code, action }) });
   149→   149→      const d = await r.json();
   150→   150→      if (r.ok) {
   151→   151→        toast({ title: d.message });
   152→   152→        setTwoFactorEnabled(action === 'enable');
   153→   153→        setShowTwoFactorSetup(false);
   154→   154→        setTwoFactorSecret('');
   155→   155→        setTwoFactorUri('');
   156→   156→        setTwoFactorSetupCode('');
   157→   157→        setTwoFactorVerifyCode('');
   158→   158→        setTwoFactorDisableCode('');
   159→   159→        if (d.token) setToken(d.token);
   160→   160→      } else { toast({ title: 'Error', description: d.error, variant: 'destructive' }); }
   161→   161→    } catch { toast({ title: 'Error', variant: 'destructive' }); }
   162→   162→    setLoading(false);
   163→   163→  };
   164→   164→
   165→   165→  const handleSaveSettings = async () => {
   166→   166→    setLoading(true);
   167→   167→    const res = await fetch('/api/settings', { method: 'PUT', headers: h(), body: JSON.stringify({ ...settings, aboutStats: JSON.stringify(aboutStats), aboutImages: JSON.stringify(aboutImages) }) });
   168→   168→    if (res.ok) toast({ title: 'Settings saved' }); else toast({ title: 'Error', variant: 'destructive' });
   169→   169→    setLoading(false);
   170→   170→  };
   171→   171→
   172→   172→  const handleSaveReel = async () => {
   173→   173→    if (!editingReel?.reelUrl) { toast({ title: 'Reel URL is required', variant: 'destructive' }); return; }
   174→   174→    setLoading(true);
   175→   175→    if (editingReel.id) await fetch('/api/reels', { method: 'PUT', headers: h(), body: JSON.stringify(editingReel) });
   176→   176→    else await fetch('/api/reels', { method: 'POST', headers: h(), body: JSON.stringify(editingReel) });
   177→   177→    toast({ title: editingReel.id ? 'Reel updated' : 'Reel added' });
   178→   178→    setEditingReel(null); setReelFormOpen(false); void fetchData(); setLoading(false);
   179→   179→  };
   180→   180→
   181→   181→  const handleDeleteReel = async (id: string) => { if (!confirm('Delete?')) return; await fetch(`/api/reels?id=${id}`, { method: 'DELETE', headers: h() }); toast({ title: 'Deleted' }); void fetchData(); };
   182→   182→
   183→   183→  const handleSaveSlide = async () => {
   184→   184→    if (!editingSlide?.title || !editingSlide?.subtitle || !editingSlide?.imageUrl) { toast({ title: 'Missing fields', variant: 'destructive' }); return; }
   185→   185→    setLoading(true);
   186→   186→    if (editingSlide.id) await fetch('/api/hero-slides', { method: 'PUT', headers: h(), body: JSON.stringify(editingSlide) });
   187→   187→    else await fetch('/api/hero-slides', { method: 'POST', headers: h(), body: JSON.stringify(editingSlide) });
   188→   188→    toast({ title: editingSlide.id ? 'Slide updated' : 'Slide created' });
   189→   189→    setEditingSlide(null); setSlideFormOpen(false); void fetchData(); setLoading(false);
   190→   190→  };
   191→   191→
   192→   192→  const handleDeleteSlide = async (id: string) => { if (!confirm('Delete this slide?')) return; await fetch(`/api/hero-slides?id=${id}`, { method: 'DELETE', headers: h() }); toast({ title: 'Deleted' }); void fetchData(); };
   193→   193→
   194→   194→  const handleSaveService = async () => {
   195→   195→    if (!editingService?.title || !editingService?.description) { toast({ title: 'Missing fields', variant: 'destructive' }); return; }
   196→   196→    setLoading(true);
   197→   197→    if (editingService.id) await fetch('/api/services', { method: 'PUT', headers: h(), body: JSON.stringify(editingService) });
   198→   198→    else await fetch('/api/services', { method: 'POST', headers: h(), body: JSON.stringify(editingService) });
   199→   199→    toast({ title: editingService.id ? 'Service updated' : 'Service created' });
   200→   200→    setEditingService(null); setServiceFormOpen(false); void fetchData(); setLoading(false);
   201→   201→  };
   202→   202→
   203→   203→  const handleDeleteService = async (id: string) => { if (!confirm('Delete this service?')) return; await fetch(`/api/services?id=${id}`, { method: 'DELETE', headers: h() }); toast({ title: 'Deleted' }); void fetchData(); };
   204→   204→
   205→   205→  const handleSaveTestimonial = async () => {
   206→   206→    if (!editingTestimonial?.name || !editingTestimonial?.content) { toast({ title: 'Missing fields', variant: 'destructive' }); return; }
   207→   207→    setLoading(true);
   208→   208→    const headers = h();
   209→   209→    if (editingTestimonial.id) await fetch('/api/testimonials', { method: 'PUT', headers, body: JSON.stringify(editingTestimonial) });
   210→   210→    else await fetch('/api/testimonials', { method: 'POST', headers, body: JSON.stringify(editingTestimonial) });
   211→   211→    toast({ title: editingTestimonial.id ? 'Review updated' : 'Review created' });
   212→   212→    setEditingTestimonial(null); setTestimonialFormOpen(false); void fetchData(); setLoading(false);
   213→   213→  };
   214→   214→
   215→   215→  const handleDeleteTestimonial = async (id: string) => { if (!confirm('Delete this review?')) return; await fetch(`/api/testimonials?id=${id}`, { method: 'DELETE', headers: h() }); toast({ title: 'Deleted' }); void fetchData(); };
   216→   216→
   217→   217→  const handleSavePartner = async () => {
   218→   218→    if (!editingPartner?.name || !editingPartner?.imageUrl) { toast({ title: 'Missing fields', variant: 'destructive' }); return; }
   219→   219→    setLoading(true);
   220→   220→    const headers = h();
   221→   221→    if (editingPartner.id) await fetch('/api/partners', { method: 'PUT', headers, body: JSON.stringify(editingPartner) });
   222→   222→    else await fetch('/api/partners', { method: 'POST', headers, body: JSON.stringify(editingPartner) });
   223→   223→    toast({ title: editingPartner.id ? 'Partner updated' : 'Partner created' });
   224→   224→    setEditingPartner(null); setPartnerFormOpen(false); void fetchData(); setLoading(false);
   225→   225→  };
   226→   226→
   227→   227→  const handleDeletePartner = async (id: string) => { if (!confirm('Delete this partner?')) return; await fetch(`/api/partners?id=${id}`, { method: 'DELETE', headers: h() }); toast({ title: 'Deleted' }); void fetchData(); };
   228→   228→
   229→   229→  const handleMarkRead = async (id: string) => {
   230→   230→    await fetch(`/api/contact?markRead=${id}`, { method: 'GET', headers: h() });
   231→   231→    void fetchData();
   232→   232→  };
   233→   233→
   234→   234→  const handleDeleteMessage = async (id: string) => { if (!confirm('Delete this message?')) return; await fetch(`/api/contact?id=${id}`, { method: 'DELETE', headers: h() }); toast({ title: 'Deleted' }); void fetchData(); };
   235→   235→
   236→   236→  const handleChangePassword = async () => {
   237→   237→    if (newPwd !== confirmPwd) { toast({ title: 'Passwords do not match', variant: 'destructive' }); return; }
   238→   238→    if (newPwd.length < 8) { toast({ title: 'Min 8 characters, with uppercase, lowercase, number & special char', variant: 'destructive' }); return; }
   239→   239→    setLoading(true);
   240→   240→    const res = await fetch('/api/auth/change-password', { method: 'POST', headers: h(), body: JSON.stringify({ currentPassword: currentPwd, newPassword: newPwd }) });
   241→   241→    const d = await res.json();
   242→   242→    if (res.ok) { toast({ title: 'Password changed' }); setCurrentPwd(''); setNewPwd(''); setConfirmPwd(''); }
   243→   243→    else toast({ title: 'Error', description: d.error, variant: 'destructive' });
   244→   244→    setLoading(false);
   245→   245→  };
   246→   246→
   247→   247→  if (!open) return null;
   248→   248→
   249→   249→  // LOGIN
   250→   250→  if (!token) {
   251→   251→    return (
   252→   252→      <div className="fixed inset-0 z-[100] bg-black/60 backdrop-blur-sm flex items-center justify-center p-4">
   253→   253→        <div className="bg-white rounded-2xl shadow-2xl w-full max-w-md p-8">
   254→   254→          <div className="flex justify-between items-center mb-6">
   255→   255→            <h2 className="text-2xl font-bold text-slate-900">Admin Login</h2>
   256→   256→            <button onClick={onClose} className="p-2 hover:bg-slate-100 rounded-lg transition-colors"><X size={20} className="text-slate-500" /></button>
   257→   257→          </div>
   258→   258→          {loginError && <div className="mb-4 p-3 bg-red-50 border border-red-200 rounded-lg text-sm text-red-600">{loginError}</div>}
   259→   259→          {twoFactorRequired && (
   260→   260→            <div className="mb-4 p-3 bg-amber-50 border border-amber-200 rounded-lg text-sm text-amber-700 flex items-center gap-2">
   261→   261→              <SmartphoneNfc size={16} className="flex-shrink-0" />
   262→   262→              Two-factor authentication required. Enter the code from your authenticator app.
   263→   263→            </div>
   264→   264→          )}
   265→   265→          <div className="space-y-4">
   266→   266→            {!twoFactorRequired && (
   267→   267→              <>
   268→   268→                <div><Label htmlFor="email">Email</Label><Input id="email" type="email" placeholder="Enter your email" value={email} onChange={(e) => setEmail(e.target.value)} className="mt-1.5" /></div>
   269→   269→                <div><Label htmlFor="password">Password</Label><Input id="password" type="password" placeholder="Enter your password" value={password} onChange={(e) => setPassword(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && handleLogin()} className="mt-1.5" /></div>
   270→   270→              </>
   271→   271→            )}
   272→   272→            {twoFactorRequired && (
   273→   273→              <div>
   274→   274→                <Label htmlFor="twofa">Authentication Code</Label>
   275→   275→                <Input id="twofa" type="text" placeholder="6-digit code" value={twoFactorCode} onChange={(e) => setTwoFactorCode(e.target.value.replace(/[^0-9]/g, '').slice(0, 6))} onKeyDown={(e) => e.key === 'Enter' && handleLogin()} className="mt-1.5 text-center text-2xl tracking-[0.5em] font-mono" maxLength={6} autoFocus />
   276→   276→                <button onClick={() => { setTwoFactorRequired(false); setPendingToken(null); setTwoFactorCode(''); setLoginError(''); }} className="mt-2 text-xs text-slate-500 hover:text-slate-700 underline">Back to password login</button>
   277→   277→              </div>
   278→   278→            )}
   279→   279→            <Button onClick={handleLogin} disabled={loginLoading || (!twoFactorRequired && (!email || !password)) || (twoFactorRequired && twoFactorCode.length !== 6)} className="w-full bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl h-11">
   280→   280→              {loginLoading && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}{twoFactorRequired ? 'Verify Code' : 'Sign In'}
   281→   281→            </Button>
   282→   282→          </div>
   283→   283→        </div>
   284→   284→      </div>
   285→   285→    );
   286→   286→  }
   287→   287→
   288→   288→  // DASHBOARD
   289→   289→  const tabs: { key: Tab; label: string; icon: React.ReactNode }[] = [
   290→   290→    { key: 'settings', label: 'Settings', icon: <Settings size={18} /> },
   291→   291→    { key: 'slides', label: 'Slides', icon: <ImageIcon size={18} /> },
   292→   292→    { key: 'services', label: 'Services', icon: <Layers size={18} /> },
   293→   293→    { key: 'reviews', label: 'Reviews', icon: <Star size={18} /> },
   294→   294→    { key: 'partners', label: 'Partners', icon: <Shield size={18} /> },
   295→   295→    { key: 'reels', label: 'Reels', icon: <Film size={18} /> },
   296→   296→    { key: 'messages', label: 'Messages', icon: <MessageSquare size={18} /> },
   297→   297→    { key: 'password', label: 'Password', icon: <KeyRound size={18} /> },
   298→   298→    { key: 'security', label: 'Security', icon: <Shield size={18} /> },
   299→   299→  ];
   300→   300→
   301→   301→  const unreadCount = messages.filter(m => !m.read).length;
   302→   302→
   303→   303→  return (
   304→   304→    <div className="fixed inset-0 z-[100] bg-black/60 backdrop-blur-sm flex items-center justify-center p-2 sm:p-4">
   305→   305→      <div className="bg-white rounded-2xl shadow-2xl w-full max-w-5xl max-h-[92vh] flex flex-col overflow-hidden">
   306→   306→        {/* Header */}
   307→   307→        <div className="flex items-center justify-between px-6 py-4 border-b">
   308→   308→          <div className="flex items-center gap-3">
   309→   309→            <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-slate-900"><span className="text-sm font-bold text-white">A</span></div>
   310→   310→            <h2 className="text-lg font-bold text-slate-900">Admin Panel</h2>
   311→   311→          </div>
   312→   312→          <div className="flex items-center gap-2">
   313→   313→            <Button variant="ghost" size="sm" onClick={() => { setToken(null); setEmail(''); setPassword(''); setActiveTab('settings'); }} className="text-slate-500 hover:text-red-500"><LogOut size={16} className="mr-1.5" />Logout</Button>
   314→   314→            <button onClick={onClose} className="p-2 hover:bg-slate-100 rounded-lg transition-colors"><X size={20} className="text-slate-500" /></button>
   315→   315→          </div>
   316→   316→        </div>
   317→   317→
   318→   318→        {/* Tabs */}
   319→   319→        <div className="flex border-b px-4 overflow-x-auto">
   320→   320→          {tabs.map((tab) => (
   321→   321→            <button key={tab.key} onClick={() => { setActiveTab(tab.key); if (tab.key === 'messages') void fetchData(); }}
   322→   322→              className={`flex items-center gap-1.5 px-3 py-3 text-sm font-medium border-b-2 transition-colors whitespace-nowrap ${activeTab === tab.key ? 'border-emerald-500 text-emerald-600' : 'border-transparent text-slate-500 hover:text-slate-700'}`}>
   323→   323→              {tab.icon} {tab.label}
   324→   324→              {tab.key === 'messages' && unreadCount > 0 && <span className="ml-1 px-1.5 py-0.5 text-xs bg-red-500 text-white rounded-full">{unreadCount}</span>}
   325→   325→            </button>
   326→   326→          ))}
   327→   327→        </div>
   328→   328→
   329→   329→        {/* Content */}
   330→   330→        <div className="flex-1 overflow-y-auto custom-scrollbar p-6">
   331→   331→
   332→   332→          {/* SETTINGS */}
   333→   333→          {activeTab === 'settings' && (
   334→   334→            <div className="space-y-6 max-w-2xl">
   335→   335→              <h3 className="text-lg font-semibold text-slate-900">Site Settings</h3>
   336→   336→              <div className="space-y-4">
   337→   337→                <div><Label>Site Name</Label><Input value={settings.siteName || ''} onChange={(e) => setSettings({ ...settings, siteName: e.target.value })} className="mt-1.5" /></div>
   338→   338→                <div><Label>Tagline</Label><Input value={settings.tagline || ''} onChange={(e) => setSettings({ ...settings, tagline: e.target.value })} className="mt-1.5" /></div>
   339→   339→                <div><Label>WhatsApp Number</Label><Input value={settings.whatsappNumber || ''} onChange={(e) => setSettings({ ...settings, whatsappNumber: e.target.value })} className="mt-1.5" /></div>
   340→   340→                <div><Label>Contact Email</Label><Input value={settings.contactEmail || ''} onChange={(e) => setSettings({ ...settings, contactEmail: e.target.value })} className="mt-1.5" /></div>
   341→   341→                <div><Label>About Title</Label><Input value={settings.aboutTitle || ''} onChange={(e) => setSettings({ ...settings, aboutTitle: e.target.value })} className="mt-1.5" /></div>
   342→   342→                <div><Label>About Description</Label><Textarea value={settings.aboutDescription || ''} onChange={(e) => setSettings({ ...settings, aboutDescription: e.target.value })} className="mt-1.5" rows={4} /></div>
   343→   343→                <div>
   344→   344→                  <div className="flex justify-between items-center mb-3"><Label>Statistics</Label><Button size="sm" variant="outline" onClick={() => setAboutStats([...aboutStats, { label: '', value: '' }])} className="text-xs"><Plus size={14} className="mr-1" />Add</Button></div>
   345→   345→                  {aboutStats.map((stat, i) => (
   346→   346→                    <div key={i} className="flex gap-2 mb-2">
   347→   347→                      <Input value={stat.label} onChange={(e) => { const n = [...aboutStats]; n[i] = { ...n[i], label: e.target.value }; setAboutStats(n); }} placeholder="Label" className="flex-1" />
   348→   348→                      <Input value={stat.value} onChange={(e) => { const n = [...aboutStats]; n[i] = { ...n[i], value: e.target.value }; setAboutStats(n); }} placeholder="Value" className="w-32" />
   349→   349→                      <Button size="icon" variant="ghost" onClick={() => setAboutStats(aboutStats.filter((_, idx) => idx !== i))} className="text-red-400 hover:text-red-600 hover:bg-red-50 flex-shrink-0"><Trash2 size={16} /></Button>
   350→   350→                    </div>
   351→   351→                  ))}
   352→   352→                </div>
   353→   353→                <div><Label>Instagram URL</Label><Input value={settings.instagramUrl || ''} onChange={(e) => setSettings({ ...settings, instagramUrl: e.target.value })} className="mt-1.5" placeholder="https://www.instagram.com/..." /></div>
   354→   354→                <div><Label>TikTok URL</Label><Input value={settings.tiktokUrl || ''} onChange={(e) => setSettings({ ...settings, tiktokUrl: e.target.value })} className="mt-1.5" placeholder="https://www.tiktok.com/@..." /></div>
   355→   355→                <div><Label>YouTube URL</Label><Input value={settings.youtubeUrl || ''} onChange={(e) => setSettings({ ...settings, youtubeUrl: e.target.value })} className="mt-1.5" placeholder="https://www.youtube.com/..." /></div>
   356→   356→                <div><Label>CTA Title</Label><Input value={settings.ctaTitle || ''} onChange={(e) => setSettings({ ...settings, ctaTitle: e.target.value })} className="mt-1.5" /></div>
   357→   357→                <div><Label>CTA Description</Label><Textarea value={settings.ctaDescription || ''} onChange={(e) => setSettings({ ...settings, ctaDescription: e.target.value })} className="mt-1.5" rows={3} /></div>
   358→   358→                <div><Label>CTA Button Text</Label><Input value={settings.ctaButtonText || ''} onChange={(e) => setSettings({ ...settings, ctaButtonText: e.target.value })} className="mt-1.5" /></div>
   359→   359→                <div>
   360→   360→                  <div className="flex justify-between items-center mb-3"><Label>About Images</Label><Button size="sm" variant="outline" onClick={() => setAboutImages([...aboutImages, ''])} className="text-xs"><Plus size={14} className="mr-1" />Add</Button></div>
   361→   361→                  <div className="space-y-2">
   362→   362→                    {aboutImages.map((url, i) => (
   363→   363→                      <div key={i} className="flex gap-2"><Input value={url} onChange={(e) => { const n = [...aboutImages]; n[i] = e.target.value; setAboutImages(n); }} placeholder={`Image ${i + 1} URL`} className="flex-1" /><Button size="icon" variant="ghost" onClick={() => setAboutImages(aboutImages.filter((_, idx) => idx !== i))} className="text-red-400 hover:text-red-600 hover:bg-red-50 flex-shrink-0"><Trash2 size={16} /></Button></div>
   364→   364→                    ))}
   365→   365→                  </div>
   366→   366→                </div>
   367→   367→              </div>
   368→   368→              <Button onClick={handleSaveSettings} disabled={loading} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl">{loading && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}<Save size={16} className="mr-2" />Save Settings</Button>
   369→   369→            </div>
   370→   370→          )}
   371→   371→
   372→   372→          {/* SLIDES */}
   373→   373→          {activeTab === 'slides' && (
   374→   374→            <div className="space-y-6">
   375→   375→              <div className="flex justify-between items-center">
   376→   376→                <h3 className="text-lg font-semibold text-slate-900">Hero Slides</h3>
   377→   377→                <Button onClick={() => { setEditingSlide({ title: '', subtitle: '', imageUrl: '', ctaText: 'Get Started', ctaLink: '#contact', order: slides.length, active: true }); setSlideFormOpen(true); }} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm"><Plus size={16} className="mr-1.5" />Add Slide</Button>
   378→   378→              </div>
   379→   379→              {slideFormOpen && editingSlide && (
   380→   380→                <div className="bg-slate-50 rounded-xl p-6 border space-y-4">
   381→   381→                  <h4 className="font-semibold text-slate-900">{editingSlide.id ? 'Edit' : 'New'} Slide</h4>
   382→   382→                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
   383→   383→                    <div className="sm:col-span-2"><Label>Title</Label><Input value={editingSlide.title || ''} onChange={(e) => setEditingSlide({ ...editingSlide, title: e.target.value })} className="mt-1.5" /></div>
   384→   384→                    <div className="sm:col-span-2"><Label>Subtitle</Label><Textarea value={editingSlide.subtitle || ''} onChange={(e) => setEditingSlide({ ...editingSlide, subtitle: e.target.value })} className="mt-1.5" rows={2} /></div>
   385→   385→                    <div className="sm:col-span-2"><Label>Image URL</Label><Input value={editingSlide.imageUrl || ''} onChange={(e) => setEditingSlide({ ...editingSlide, imageUrl: e.target.value })} className="mt-1.5" /></div>
   386→   386→                    <div><Label>CTA Text</Label><Input value={editingSlide.ctaText || ''} onChange={(e) => setEditingSlide({ ...editingSlide, ctaText: e.target.value })} className="mt-1.5" /></div>
   387→   387→                    <div><Label>CTA Link</Label><Input value={editingSlide.ctaLink || ''} onChange={(e) => setEditingSlide({ ...editingSlide, ctaLink: e.target.value })} className="mt-1.5" /></div>
   388→   388→                    <div><Label>Order</Label><Input type="number" value={editingSlide.order || 0} onChange={(e) => setEditingSlide({ ...editingSlide, order: parseInt(e.target.value) || 0 })} className="mt-1.5" /></div>
   389→   389→                    <div className="flex items-end pb-1"><div className="flex items-center gap-2"><Switch checked={editingSlide.active ?? true} onCheckedChange={(c) => setEditingSlide({ ...editingSlide, active: c })} /><Label>Active</Label></div></div>
   390→   390→                  </div>
   391→   391→                  <div className="flex gap-2">
   392→   392→                    <Button onClick={handleSaveSlide} disabled={loading} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm">{loading && <Loader2 className="mr-1.5 h-3.5 w-3.5 animate-spin" />}Save</Button>
   393→   393→                    <Button variant="outline" onClick={() => { setSlideFormOpen(false); setEditingSlide(null); }} size="sm">Cancel</Button>
   394→   394→                  </div>
   395→   395→                </div>
   396→   396→              )}
   397→   397→              <div className="space-y-3">
   398→   398→                {slides.map((s) => (
   399→   399→                  <div key={s.id} className="flex items-center gap-4 p-4 bg-slate-50 rounded-xl border">
   400→   400→                    <div className="w-20 h-14 rounded-lg overflow-hidden bg-slate-200 flex-shrink-0"><img src={s.imageUrl} alt={s.title} className="w-full h-full object-cover" /></div>
   401→   401→                    <div className="flex-1 min-w-0"><p className="font-medium text-slate-900 truncate">{s.title}</p><p className="text-sm text-slate-500 truncate">{s.subtitle}</p></div>
   402→   402→                    <span className={`text-xs px-2 py-1 rounded-full ${s.active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-200 text-slate-500'}`}>{s.active ? 'Active' : 'Inactive'}</span>
   403→   403→                    <Button size="icon" variant="ghost" onClick={() => { setEditingSlide(s); setSlideFormOpen(true); }} className="h-8 w-8 text-slate-500 hover:text-emerald-600"><Edit3 size={15} /></Button>
   404→   404→                    <Button size="icon" variant="ghost" onClick={() => handleDeleteSlide(s.id)} className="h-8 w-8 text-slate-500 hover:text-red-500"><Trash2 size={15} /></Button>
   405→   405→                  </div>
   406→   406→                ))}
   407→   407→              </div>
   408→   408→            </div>
   409→   409→          )}
   410→   410→
   411→   411→          {/* SERVICES */}
   412→   412→          {activeTab === 'services' && (
   413→   413→            <div className="space-y-6">
   414→   414→              <div className="flex justify-between items-center">
   415→   415→                <h3 className="text-lg font-semibold text-slate-900">Services</h3>
   416→   416→                <Button onClick={() => { setEditingService({ title: '', description: '', icon: 'TrendingUp', order: services.length, active: true }); setServiceFormOpen(true); }} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm"><Plus size={16} className="mr-1.5" />Add Service</Button>
   417→   417→              </div>
   418→   418→              {serviceFormOpen && editingService && (
   419→   419→                <div className="bg-slate-50 rounded-xl p-6 border space-y-4">
   420→   420→                  <h4 className="font-semibold text-slate-900">{editingService.id ? 'Edit' : 'New'} Service</h4>
   421→   421→                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
   422→   422→                    <div className="sm:col-span-2"><Label>Title</Label><Input value={editingService.title || ''} onChange={(e) => setEditingService({ ...editingService, title: e.target.value })} className="mt-1.5" /></div>
   423→   423→                    <div className="sm:col-span-2"><Label>Description</Label><Textarea value={editingService.description || ''} onChange={(e) => setEditingService({ ...editingService, description: e.target.value })} className="mt-1.5" rows={3} /></div>
   424→   424→                    <div><Label>Icon</Label><select value={editingService.icon || 'TrendingUp'} onChange={(e) => setEditingService({ ...editingService, icon: e.target.value })} className="mt-1.5 w-full h-10 rounded-lg border border-input bg-white px-3 text-sm">{Object.keys(iconMap).map((n) => <option key={n} value={n}>{n}</option>)}</select></div>
   425→   425→                    <div><Label>Order</Label><Input type="number" value={editingService.order || 0} onChange={(e) => setEditingService({ ...editingService, order: parseInt(e.target.value) || 0 })} className="mt-1.5" /></div>
   426→   426→                    <div className="flex items-end pb-1"><div className="flex items-center gap-2"><Switch checked={editingService.active ?? true} onCheckedChange={(c) => setEditingService({ ...editingService, active: c })} /><Label>Active</Label></div></div>
   427→   427→                  </div>
   428→   428→                  <div className="flex gap-2">
   429→   429→                    <Button onClick={handleSaveService} disabled={loading} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm">{loading && <Loader2 className="mr-1.5 h-3.5 w-3.5 animate-spin" />}Save</Button>
   430→   430→                    <Button variant="outline" onClick={() => { setServiceFormOpen(false); setEditingService(null); }} size="sm">Cancel</Button>
   431→   431→                  </div>
   432→   432→                </div>
   433→   433→              )}
   434→   434→              <div className="space-y-3">
   435→   435→                {services.map((s) => { const Icon = iconMap[s.icon] || TrendingUp; return (
   436→   436→                  <div key={s.id} className="flex items-center gap-4 p-4 bg-slate-50 rounded-xl border">
   437→   437→                    <div className="w-10 h-10 rounded-lg bg-emerald-50 flex items-center justify-center flex-shrink-0"><Icon size={20} className="text-emerald-600" /></div>
   438→   438→                    <div className="flex-1 min-w-0"><p className="font-medium text-slate-900">{s.title}</p><p className="text-sm text-slate-500 truncate">{s.description}</p></div>
   439→   439→                    <span className={`text-xs px-2 py-1 rounded-full ${s.active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-200 text-slate-500'}`}>{s.active ? 'Active' : 'Inactive'}</span>
   440→   440→                    <Button size="icon" variant="ghost" onClick={() => { setEditingService(s); setServiceFormOpen(true); }} className="h-8 w-8 text-slate-500 hover:text-emerald-600"><Edit3 size={15} /></Button>
   441→   441→                    <Button size="icon" variant="ghost" onClick={() => handleDeleteService(s.id)} className="h-8 w-8 text-slate-500 hover:text-red-500"><Trash2 size={15} /></Button>
   442→   442→                  </div>
   443→   443→                ); })}
   444→   444→              </div>
   445→   445→            </div>
   446→   446→          )}
   447→   447→
   448→   448→          {/* REVIEWS */}
   449→   449→          {activeTab === 'reviews' && (
   450→   450→            <div className="space-y-6">
   451→   451→              <div className="flex justify-between items-center">
   452→   452→                <h3 className="text-lg font-semibold text-slate-900">Client Reviews</h3>
   453→   453→                <Button onClick={() => { setEditingTestimonial({ name: '', role: '', company: '', content: '', rating: 5, imageUrl: '', order: testimonials.length, active: true }); setTestimonialFormOpen(true); }} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm"><Plus size={16} className="mr-1.5" />Add Review</Button>
   454→   454→              </div>
   455→   455→              {testimonialFormOpen && editingTestimonial && (
   456→   456→                <div className="bg-slate-50 rounded-xl p-6 border space-y-4">
   457→   457→                  <h4 className="font-semibold text-slate-900">{editingTestimonial.id ? 'Edit' : 'New'} Review</h4>
   458→   458→                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
   459→   459→                    <div><Label>Name *</Label><Input value={editingTestimonial.name || ''} onChange={(e) => setEditingTestimonial({ ...editingTestimonial, name: e.target.value })} className="mt-1.5" /></div>
   460→   460→                    <div><Label>Role</Label><Input value={editingTestimonial.role || ''} onChange={(e) => setEditingTestimonial({ ...editingTestimonial, role: e.target.value })} className="mt-1.5" /></div>
   461→   461→                    <div><Label>Company</Label><Input value={editingTestimonial.company || ''} onChange={(e) => setEditingTestimonial({ ...editingTestimonial, company: e.target.value })} className="mt-1.5" /></div>
   462→   462→                    <div><Label>Rating (1-5)</Label><Input type="number" min="1" max="5" value={editingTestimonial.rating || 5} onChange={(e) => setEditingTestimonial({ ...editingTestimonial, rating: Math.min(5, Math.max(1, parseInt(e.target.value) || 5)) })} className="mt-1.5" /></div>
   463→   463→                    <div className="sm:col-span-2"><Label>Review Content *</Label><Textarea value={editingTestimonial.content || ''} onChange={(e) => setEditingTestimonial({ ...editingTestimonial, content: e.target.value })} className="mt-1.5" rows={3} /></div>
   464→   464→                    <div className="sm:col-span-2"><Label>Image URL (optional)</Label><Input value={editingTestimonial.imageUrl || ''} onChange={(e) => setEditingTestimonial({ ...editingTestimonial, imageUrl: e.target.value })} className="mt-1.5" /></div>
   465→   465→                    <div><Label>Order</Label><Input type="number" value={editingTestimonial.order || 0} onChange={(e) => setEditingTestimonial({ ...editingTestimonial, order: parseInt(e.target.value) || 0 })} className="mt-1.5" /></div>
   466→   466→                    <div className="flex items-end pb-1"><div className="flex items-center gap-2"><Switch checked={editingTestimonial.active ?? true} onCheckedChange={(c) => setEditingTestimonial({ ...editingTestimonial, active: c })} /><Label>Active</Label></div></div>
   467→   467→                  </div>
   468→   468→                  <div className="flex gap-2">
   469→   469→                    <Button onClick={handleSaveTestimonial} disabled={loading} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm">{loading && <Loader2 className="mr-1.5 h-3.5 w-3.5 animate-spin" />}Save</Button>
   470→   470→                    <Button variant="outline" onClick={() => { setTestimonialFormOpen(false); setEditingTestimonial(null); }} size="sm">Cancel</Button>
   471→   471→                  </div>
   472→   472→                </div>
   473→   473→              )}
   474→   474→              <div className="space-y-3">
   475→   475→                {testimonials.map((t) => (
   476→   476→                  <div key={t.id} className="p-4 bg-slate-50 rounded-xl border">
   477→   477→                    <div className="flex items-start justify-between gap-4">
   478→   478→                      <div className="flex-1 min-w-0">
   479→   479→                        <div className="flex items-center gap-2 mb-1">
   480→   480→                          <p className="font-semibold text-slate-900">{t.name}</p>
   481→   481→                          <div className="flex gap-0.5">{Array.from({ length: 5 }).map((_, i) => <Star key={i} size={12} className={i < t.rating ? 'text-amber-400 fill-amber-400' : 'text-slate-200'} />)}</div>
   482→   482→                        </div>
   483→   483→                        <p className="text-xs text-slate-500 mb-2">{t.role}{t.company ? `, ${t.company}` : ''}</p>
   484→   484→                        <p className="text-sm text-slate-600 line-clamp-2">{t.content}</p>
   485→   485→                      </div>
   486→   486→                      <div className="flex items-center gap-2 flex-shrink-0">
   487→   487→                        <span className={`text-xs px-2 py-1 rounded-full ${t.active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-200 text-slate-500'}`}>{t.active ? 'Active' : 'Inactive'}</span>
   488→   488→                        <Button size="icon" variant="ghost" onClick={() => { setEditingTestimonial(t); setTestimonialFormOpen(true); }} className="h-8 w-8 text-slate-500 hover:text-emerald-600"><Edit3 size={15} /></Button>
   489→   489→                        <Button size="icon" variant="ghost" onClick={() => handleDeleteTestimonial(t.id)} className="h-8 w-8 text-slate-500 hover:text-red-500"><Trash2 size={15} /></Button>
   490→   490→                      </div>
   491→   491→                    </div>
   492→   492→                  </div>
   493→   493→                ))}
   494→   494→              </div>
   495→   495→            </div>
   496→   496→          )}
   497→   497→
   498→   498→          {/* PARTNERS */}
   499→   499→          {activeTab === 'partners' && (
   500→   500→            <div className="space-y-6">
   501→   501→              <div className="flex justify-between items-center">
   502→   502→                <h3 className="text-lg font-semibold text-slate-900">Trusted Partners</h3>
   503→   503→                <Button onClick={() => { setEditingPartner({ name: '', imageUrl: '', website: '', order: partners.length, active: true }); setPartnerFormOpen(true); }} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm"><Plus size={16} className="mr-1.5" />Add Partner</Button>
   504→   504→              </div>
   505→   505→              {partnerFormOpen && editingPartner && (
   506→   506→                <div className="bg-slate-50 rounded-xl p-6 border space-y-4">
   507→   507→                  <h4 className="font-semibold text-slate-900">{editingPartner.id ? 'Edit' : 'New'} Partner</h4>
   508→   508→                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
   509→   509→                    <div><Label>Name *</Label><Input value={editingPartner.name || ''} onChange={(e) => setEditingPartner({ ...editingPartner, name: e.target.value })} className="mt-1.5" /></div>
   510→   510→                    <div><Label>Website</Label><Input value={editingPartner.website || ''} onChange={(e) => setEditingPartner({ ...editingPartner, website: e.target.value })} className="mt-1.5" /></div>
   511→   511→                    <div className="sm:col-span-2"><Label>Logo Image URL *</Label><Input value={editingPartner.imageUrl || ''} onChange={(e) => setEditingPartner({ ...editingPartner, imageUrl: e.target.value })} className="mt-1.5" /></div>
   512→   512→                    <div><Label>Order</Label><Input type="number" value={editingPartner.order || 0} onChange={(e) => setEditingPartner({ ...editingPartner, order: parseInt(e.target.value) || 0 })} className="mt-1.5" /></div>
   513→   513→                    <div className="flex items-end pb-1"><div className="flex items-center gap-2"><Switch checked={editingPartner.active ?? true} onCheckedChange={(c) => setEditingPartner({ ...editingPartner, active: c })} /><Label>Active</Label></div></div>
   514→   514→                  </div>
   515→   515→                  <div className="flex gap-2">
   516→   516→                    <Button onClick={handleSavePartner} disabled={loading} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm">{loading && <Loader2 className="mr-1.5 h-3.5 w-3.5 animate-spin" />}Save</Button>
   517→   517→                    <Button variant="outline" onClick={() => { setPartnerFormOpen(false); setEditingPartner(null); }} size="sm">Cancel</Button>
   518→   518→                  </div>
   519→   519→                </div>
   520→   520→              )}
   521→   521→              <div className="space-y-3">
   522→   522→                {partners.map((p) => (
   523→   523→                  <div key={p.id} className="flex items-center gap-4 p-4 bg-slate-50 rounded-xl border">
   524→   524→                    <div className="w-16 h-12 rounded-lg overflow-hidden bg-white border flex items-center justify-center p-2 flex-shrink-0"><img src={p.imageUrl} alt={p.name} className="max-h-full max-w-full object-contain" /></div>
   525→   525→                    <div className="flex-1 min-w-0"><p className="font-medium text-slate-900">{p.name}</p>{p.website && <p className="text-xs text-slate-500 truncate">{p.website}</p>}</div>
   526→   526→                    <span className={`text-xs px-2 py-1 rounded-full ${p.active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-200 text-slate-500'}`}>{p.active ? 'Active' : 'Inactive'}</span>
   527→   527→                    <Button size="icon" variant="ghost" onClick={() => { setEditingPartner(p); setPartnerFormOpen(true); }} className="h-8 w-8 text-slate-500 hover:text-emerald-600"><Edit3 size={15} /></Button>
   528→   528→                    <Button size="icon" variant="ghost" onClick={() => handleDeletePartner(p.id)} className="h-8 w-8 text-slate-500 hover:text-red-500"><Trash2 size={15} /></Button>
   529→   529→                  </div>
   530→   530→                ))}
   531→   531→              </div>
   532→   532→            </div>
   533→   533→          )}
   534→   534→
   535→   535→          {/* MESSAGES */}
   536→   536→          {activeTab === 'messages' && (
   537→   537→            <div className="space-y-6">
   538→   538→              <div className="flex justify-between items-center">
   539→   539→                <h3 className="text-lg font-semibold text-slate-900">Contact Messages</h3>
   540→   540→                <Button onClick={() => void fetchData()} variant="outline" size="sm"><Loader2 size={14} className="mr-1" />Refresh</Button>
   541→   541→              </div>
   542→   542→              {messages.length === 0 && <p className="text-center text-slate-400 py-12">No messages yet. When visitors submit the contact form, their messages will appear here.</p>}
   543→   543→              <div className="space-y-3">
   544→   544→                {messages.map((m) => (
   545→   545→                  <div key={m.id} className={`p-4 rounded-xl border ${m.read ? 'bg-white border-slate-100' : 'bg-emerald-50 border-emerald-200'}`}>
   546→   546→                    <div className="flex items-start justify-between gap-3">
   547→   547→                      <div className="flex-1 min-w-0">
   548→   548→                        <div className="flex items-center gap-2 mb-1">
   549→   549→                          {!m.read && <span className="w-2 h-2 rounded-full bg-emerald-500 flex-shrink-0" />}
   550→   550→                          <p className={`font-semibold text-sm ${m.read ? 'text-slate-700' : 'text-slate-900'}`}>{m.name}</p>
   551→   551→                          <span className="text-xs text-slate-400">{new Date(m.createdAt).toLocaleString()}</span>
   552→   552→                        </div>
   553→   553→                        <p className="text-xs text-slate-500 mb-2">{m.email}{m.phone ? ` | ${m.phone}` : ''}{m.subject ? ` | ${m.subject}` : ''}</p>
   554→   554→                        <p className="text-sm text-slate-600">{m.message}</p>
   555→   555→                      </div>
   556→   556→                      <div className="flex flex-col gap-1 flex-shrink-0">
   557→   557→                        {!m.read && <Button size="icon" variant="ghost" onClick={() => handleMarkRead(m.id)} className="h-7 w-7 text-slate-400 hover:text-emerald-600" title="Mark read"><Eye size={14} /></Button>}
   558→   558→                        {m.read && <Button size="icon" variant="ghost" onClick={() => handleMarkRead(m.id)} className="h-7 w-7 text-slate-400 hover:text-slate-600" title="Mark unread"><EyeOff size={14} /></Button>}
   559→   559→                        <Button size="icon" variant="ghost" onClick={() => handleDeleteMessage(m.id)} className="h-7 w-7 text-slate-400 hover:text-red-500"><Trash2 size={14} /></Button>
   560→   560→                      </div>
   561→   561→                    </div>
   562→   562→                  </div>
   563→   563→                ))}
   564→   564→              </div>
   565→   565→            </div>
   566→   566→          )}
   567→   567→
   568→   568→          {/* REELS */}
   569→   569→          {activeTab === 'reels' && (
   570→   570→            <div className="space-y-6">
   571→   571→              <div className="flex justify-between items-center">
   572→   572→                <h3 className="text-lg font-semibold text-slate-900">Social Reels</h3>
   573→   573→                <Button onClick={() => { setEditingReel({ platform: 'instagram', reelUrl: '', order: reels.length, active: true }); setReelFormOpen(true); }} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm"><Plus size={16} className="mr-1.5" />Add Reel</Button>
   574→   574→              </div>
   575→   575→              {reelFormOpen && editingReel && (
   576→   576→                <div className="bg-slate-50 rounded-xl p-6 border space-y-4">
   577→   577→                  <h4 className="font-semibold text-slate-900">{editingReel.id ? 'Edit' : 'New'} Reel</h4>
   578→   578→                  <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
   579→   579→                    <div><Label>Platform</Label><select value={editingReel.platform || 'instagram'} onChange={(e) => setEditingReel({ ...editingReel, platform: e.target.value })} className="mt-1.5 w-full h-10 rounded-lg border border-input bg-white px-3 text-sm"><option value="instagram">Instagram</option><option value="tiktok">TikTok</option><option value="youtube">YouTube</option></select></div>
   580→   580→                    <div className="sm:col-span-2"><Label>Reel URL *</Label><Input value={editingReel.reelUrl || ''} onChange={(e) => setEditingReel({ ...editingReel, reelUrl: e.target.value })} className="mt-1.5" placeholder="https://www.instagram.com/reel/..." /></div>
   581→   581→                    <div><Label>Order</Label><Input type="number" value={editingReel.order || 0} onChange={(e) => setEditingReel({ ...editingReel, order: parseInt(e.target.value) || 0 })} className="mt-1.5" /></div>
   582→   582→                    <div className="flex items-end pb-1"><div className="flex items-center gap-2"><Switch checked={editingReel.active ?? true} onCheckedChange={(c) => setEditingReel({ ...editingReel, active: c })} /><Label>Active</Label></div></div>
   583→   583→                  </div>
   584→   584→                  <div className="flex gap-2">
   585→   585→                    <Button onClick={handleSaveReel} disabled={loading} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm">{loading && <Loader2 className="mr-1.5 h-3.5 w-3.5 animate-spin" />}Save</Button>
   586→   586→                    <Button variant="outline" onClick={() => { setReelFormOpen(false); setEditingReel(null); }} size="sm">Cancel</Button>
   587→   587→                  </div>
   588→   588→                </div>
   589→   589→              )}
   590→   590→              <div className="space-y-3">
   591→   591→                {reels.map((r) => (
   592→   592→                  <div key={r.id} className="flex items-center gap-4 p-4 bg-slate-50 rounded-xl border">
   593→   593→                    <div className="w-10 h-10 rounded-lg bg-pink-50 flex items-center justify-center flex-shrink-0">
   594→   594→                      {r.platform === 'tiktok' ? <Play size={18} className="text-slate-600" /> : <Camera size={18} className="text-slate-600" />}
   595→   595→                    </div>
   596→   596→                    <div className="flex-1 min-w-0"><p className="text-sm font-medium text-slate-900 truncate">{r.reelUrl}</p><p className="text-xs text-slate-400">{r.platform}</p></div>
   597→   597→                    <span className={`text-xs px-2 py-1 rounded-full ${r.active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-200 text-slate-500'}`}>{r.active ? 'Active' : 'Inactive'}</span>
   598→   598→                    <Button size="icon" variant="ghost" onClick={() => { setEditingReel(r); setReelFormOpen(true); }} className="h-8 w-8 text-slate-500 hover:text-emerald-600"><Edit3 size={15} /></Button>
   599→   599→                    <Button size="icon" variant="ghost" onClick={() => handleDeleteReel(r.id)} className="h-8 w-8 text-slate-500 hover:text-red-500"><Trash2 size={15} /></Button>
   600→   600→                  </div>
   601→   601→                ))}
   602→   602→                {reels.length === 0 && !reelFormOpen && <p className="text-center text-slate-400 py-8">No reels yet. Add Instagram or TikTok reel URLs to display them on the website.</p>}
   603→   603→              </div>
   604→   604→            </div>
   605→   605→          )}
   606→   606→
   607→   607→          {/* PASSWORD */}
   608→   608→          {activeTab === 'password' && (
   609→   609→            <div className="space-y-6 max-w-md">
   610→   610→              <h3 className="text-lg font-semibold text-slate-900">Change Password</h3>
   611→   611→              <div className="space-y-4">
   612→   612→                <div><Label>Current Password</Label><Input type="password" value={currentPwd} onChange={(e) => setCurrentPwd(e.target.value)} className="mt-1.5" /></div>
   613→   613→                <div><Label>New Password</Label><Input type="password" value={newPwd} onChange={(e) => setNewPwd(e.target.value)} className="mt-1.5" placeholder="Min 8 chars: Aa1@..." /></div>
   614→   614→                <div><Label>Confirm New Password</Label><Input type="password" value={confirmPwd} onChange={(e) => setConfirmPwd(e.target.value)} className="mt-1.5" /></div>
   615→   615→              </div>
   616→   616→              <Button onClick={handleChangePassword} disabled={loading || !currentPwd || !newPwd || !confirmPwd} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl">{loading && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}<KeyRound size={16} className="mr-2" />Change Password</Button>
   617→   617→            </div>
   618→   618→          )}
   619→   619→
   620→   620→          {/* SECURITY / 2FA */}
   621→   621→          {activeTab === 'security' && (
   622→   622→            <div className="space-y-6 max-w-lg">
   623→   623→              <h3 className="text-lg font-semibold text-slate-900">Security Settings</h3>
   624→   624→
   625→   625→              {/* 2FA Status */}
   626→   626→              <div className={`p-4 rounded-xl border ${twoFactorEnabled ? 'bg-emerald-50 border-emerald-200' : 'bg-slate-50 border-slate-200'}`}>
   627→   627→                <div className="flex items-center gap-3 mb-3">
   628→   628→                  {twoFactorEnabled ? <Lock size={20} className="text-emerald-600" /> : <Unlock size={20} className="text-slate-400" />}
   629→   629→                  <div>
   630→   630→                    <p className="font-medium text-slate-900">Two-Factor Authentication</p>
   631→   631→                    <p className="text-sm text-slate-500">{twoFactorEnabled ? 'Enabled - Your account has extra protection' : 'Disabled - Add an extra layer of security'}</p>
   632→   632→                  </div>
   633→   633→                  <span className={`ml-auto text-xs px-2.5 py-1 rounded-full font-medium ${twoFactorEnabled ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-200 text-slate-500'}`}>{twoFactorEnabled ? 'ON' : 'OFF'}</span>
   634→   634→                </div>
   635→   635→
   636→   636→                {!twoFactorEnabled && !showTwoFactorSetup && (
   637→   637→                  <Button onClick={handleSetup2FA} disabled={loading} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm">
   638→   638→                    {loading && <Loader2 className="mr-1.5 h-3.5 w-3.5 animate-spin" />}Enable 2FA
   639→   639→                  </Button>
   640→   640→                )}
   641→   641→              </div>
   642→   642→
   643→   643→              {/* 2FA Setup Flow */}
   644→   644→              {showTwoFactorSetup && !twoFactorEnabled && twoFactorUri && (
   645→   645→                <div className="bg-blue-50 border border-blue-200 rounded-xl p-6 space-y-4">
   646→   646→                  <h4 className="font-semibold text-slate-900">Step 1: Scan QR Code</h4>
   647→   647→                  <p className="text-sm text-slate-600">Open Google Authenticator, Authy, or any TOTP app and scan this QR code:</p>
   648→   648→                  <div className="bg-white p-4 rounded-lg border inline-block">
   649→   649→                    <img src={`https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=${encodeURIComponent(twoFactorUri)}`} alt="2FA QR Code" className="w-48 h-48" draggable="false" />
   650→   650→                  </div>
   651→   651→                  <div className="bg-slate-100 rounded-lg p-3">
   652→   652→                    <p className="text-xs text-slate-500 mb-1">Manual entry key:</p>
   653→   653→                    <p className="font-mono text-sm text-slate-800 select-all break-all">{twoFactorSecret}</p>
   654→   654→                  </div>
   655→   655→
   656→   656→                  <div className="border-t pt-4">
   657→   657→                    <h4 className="font-semibold text-slate-900 mb-2">Step 2: Verify Code</h4>
   658→   658→                    <p className="text-sm text-slate-600 mb-3">Enter the 6-digit code from your authenticator app to confirm setup. Test code: <span className="font-mono font-bold text-emerald-600">{twoFactorSetupCode}</span></p>
   659→   659→                    <div className="flex gap-2">
   660→   660→                      <Input type="text" placeholder="000000" value={twoFactorVerifyCode} onChange={(e) => setTwoFactorVerifyCode(e.target.value.replace(/[^0-9]/g, '').slice(0, 6))} className="w-40 text-center text-xl tracking-[0.3em] font-mono" maxLength={6} />
   661→   661→                      <Button onClick={() => handleVerify2FA('enable')} disabled={loading || twoFactorVerifyCode.length !== 6} className="bg-emerald-500 hover:bg-emerald-600 text-white rounded-xl" size="sm">
   662→   662→                        {loading && <Loader2 className="mr-1.5 h-3.5 w-3.5 animate-spin" />}Verify &amp; Enable
   663→   663→                      </Button>
   664→   664→                    </div>
   665→   665→                  </div>
   666→   666→                  <button onClick={() => { setShowTwoFactorSetup(false); setTwoFactorSecret(''); setTwoFactorUri(''); setTwoFactorSetupCode(''); setTwoFactorVerifyCode(''); }} className="text-xs text-slate-500 hover:text-slate-700 underline">Cancel setup</button>
   667→   667→                </div>
   668→   668→              )}
   669→   669→
   670→   670→              {/* Disable 2FA */}
   671→   671→              {twoFactorEnabled && (
   672→   672→                <div className="bg-red-50 border border-red-200 rounded-xl p-6 space-y-4">
   673→   673→                  <h4 className="font-semibold text-red-800">Disable Two-Factor Authentication</h4>
   674→   674→                  <p className="text-sm text-slate-600">Enter a valid 2FA code from your authenticator app to disable 2FA. This will make your account less secure.</p>
   675→   675→                  <div className="flex gap-2">
   676→   676→                    <Input type="text" placeholder="000000" value={twoFactorDisableCode} onChange={(e) => setTwoFactorDisableCode(e.target.value.replace(/[^0-9]/g, '').slice(0, 6))} className="w-40 text-center text-xl tracking-[0.3em] font-mono" maxLength={6} />
   677→   677→                    <Button onClick={() => handleVerify2FA('disable')} disabled={loading || twoFactorDisableCode.length !== 6} variant="destructive" size="sm">
   678→   678→                      {loading && <Loader2 className="mr-1.5 h-3.5 w-3.5 animate-spin" />}Disable 2FA
   679→   679→                    </Button>
   680→   680→                  </div>
   681→   681→                </div>
   682→   682→              )}
   683→   683→
   684→   684→              {/* Security Info */}
   685→   685→              <div className="bg-slate-50 border border-slate-200 rounded-xl p-6 space-y-3">
   686→   686→                <h4 className="font-semibold text-slate-900">Security Features Active</h4>
   687→   687→                <div className="grid grid-cols-1 sm:grid-cols-2 gap-3 text-sm">
   688→   688→                  <div className="flex items-center gap-2 text-emerald-700"><div className="w-2 h-2 rounded-full bg-emerald-500" />HMAC-signed auth tokens</div>
   689→   689→                  <div className="flex items-center gap-2 text-emerald-700"><div className="w-2 h-2 rounded-full bg-emerald-500" />24-hour session expiry</div>
   690→   690→                  <div className="flex items-center gap-2 text-emerald-700"><div className="w-2 h-2 rounded-full bg-emerald-500" />PBKDF2 hashing (600K iterations)</div>
   691→   691→                  <div className="flex items-center gap-2 text-emerald-700"><div className="w-2 h-2 rounded-full bg-emerald-500" />Rate limiting on all endpoints</div>
   692→   692→                  <div className="flex items-center gap-2 text-emerald-700"><div className="w-2 h-2 rounded-full bg-emerald-500" />Input sanitization (XSS prevention)</div>
   693→   693→                  <div className="flex items-center gap-2 text-emerald-700"><div className="w-2 h-2 rounded-full bg-emerald-500" />Account lockout (5 failed attempts)</div>
   694→   694→                  <div className="flex items-center gap-2 text-emerald-700"><div className="w-2 h-2 rounded-full bg-emerald-500" />Content Security Policy (CSP)</div>
   695→   695→                  <div className="flex items-center gap-2 text-emerald-700"><div className="w-2 h-2 rounded-full bg-emerald-500" />Secure HTTP headers (HSTS)</div>
   696→   696→                  <div className="flex items-center gap-2 text-emerald-700"><div className="w-2 h-2 rounded-full bg-emerald-500" />Timing-safe comparisons</div>
   697→   697→                  <div className="flex items-center gap-2 text-emerald-700"><div className="w-2 h-2 rounded-full bg-emerald-500" />Content copy protection</div>
   698→   698→                </div>
   699→   699→              </div>
   700→   700→            </div>
   701→   701→          )}
   702→   702→        </div>
   703→   703→      </div>
   704→   704→    </div>
   705→   705→  );
   706→   706→}